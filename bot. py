import telebot
import google.generativeai as genai
import os

# ضع مفتاح Google API الخاص بك هنا
genai.configure(api_key="AQ.Ab8RN6KQwV5TEku8UhFZDRYnyW498FzuyggtZl1IAzy9ka2MNQ")

# ضع التوكن الخاص ببوت تليجرام هنا
bot = telebot.TeleBot("8989715708:AAFz6LnnDTG8pDvY30UWAhIOWuXG4j3sKNs")

model = genai.GenerativeModel('gemini-1.5-flash')

@bot.message_handler(func=lambda message: True)
def handle_message(message):
    try:
        response = model.generate_content(message.text)
        bot.reply_to(message, response.text)
    except Exception as e:
        bot.reply_to(message, "حدث خطأ تقني، يرجى المحاولة لاحقاً.")

bot.polling()
